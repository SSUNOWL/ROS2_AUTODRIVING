# 1. 홈 디렉토리로 이동 후 작업 폴더 생성 (관리용)
cd ~
mkdir -p libraries && cd libraries

# 2. OSQP 소스 다운로드 (v0.6.3 버전 사용 - 호환성 제일 좋음)
git clone --recursive https://github.com/osqp/osqp
cd osqp
git checkout v0.6.3
git submodule update --init --recursive

# 3. 빌드 및 설치
mkdir build && cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/usr/local
make
sudo make install

---------------
cd ~/libraries

# 1. OsqpEigen 소스 다운로드
git clone https://github.com/robotology/osqp-eigen
cd osqp-eigen

# 2. 빌드 및 설치
mkdir build && cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/usr/local
make
sudo make install

sudo ldconfig