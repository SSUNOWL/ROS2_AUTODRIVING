amcl:
  ros__parameters:
    use_sim_time: True

bt_navigator:
  ros__parameters:
    use_sim_time: True
    global_frame: map
    robot_base_frame: ego_racecar/base_link
    odom_topic: /ego_racecar/odom
    bt_loop_duration: 10
    default_server_timeout: 20
    enable_groot_monitoring: True
    groot_zmq_publisher_port: 1666
    groot_zmq_server_port: 1667
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_recovery_node_bt_node
controller_server:
  ros__parameters:
    use_sim_time: True
    controller_frequency: 10.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.0 # Non-holonomic 차량이므로 0.0 유지
    min_theta_velocity_threshold: 0.001
    controller_plugins: ["FollowPath"]

    # 🚀 Regulated Pure Pursuit Controller 적용
    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      
      # --- Pure Pursuit Parameters ---
      # F1Tenth에 적합한 추종 거리 설정 (너무 길면 코너를 자르고, 너무 짧으면 불안정)
      lookahead_dist: 0.6 
      
      # --- Kinematic & Goal Constraints ---
      desired_linear_vel: 1.5             # 최대 주행 목표 속도
      min_linear_vel: 0.1               # 최소 선속도
      
      # 전역 플래너(DUBIN)의 minimum_turning_radius와 일치 (0.50m)
      # 🚀 전역/지역 곡률 호환성 확보
      min_turning_radius: 0.50           

      # 가속도 제약 (이전 DWB 설정 유지)
      max_linear_accel: 2.5
      max_angular_accel: 3.2
      
      # 목표 도달 허용 오차 (정밀도 유지를 위해 0.15m 유지)
      xy_goal_tolerance: 0.15
      yaw_goal_tolerance: 0.15
      
      # --- Obstacle Regulation ---
      # 장애물과 가까워질 때 속도를 줄이는 최소 거리 설정
      obstacle_safety_margin: 0.15
      
      # Costmap Inflation Radius와 동일하게 설정
      inflation_cost_scale: 0.35 

      # 후진 주행 허용 여부 (DUBIN 전진 모델을 사용하므로 False 유지)
      allow_reversing: False
      
      # 기타 설정
      use_interpolation: True
      use_cost_regulated_speed: True    # 장애물 가까이에서 속도 조절 기능 활성화
      regulated_linear_scaling_min_speed: 0.4 # 속도 조절 시 최소 속도
      regulated_linear_scaling_min_radius: 0.9 # 곡률 조절 시 최소 회전 반경
      
      # 궤적 추종 속도 조절 시 사용할 비용맵 유형 (LOCAL로 설정)
      cost_map_client_topic: /local_costmap/costmap_raw
      
      transform_tolerance: 0.2

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: map
      robot_base_frame: ego_racecar/base_link
      use_sim_time: True
      rolling_window: true
      width: 5
      height: 5
      resolution: 0.05
      robot_radius: 0.15  # F1Tenth 차량 반지름
      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 10.0
          raytrace_min_range: 0.0
          obstacle_max_range: 9.5
          obstacle_min_range: 0.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.35
      always_send_full_costmap: True

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: ego_racecar/base_link
      use_sim_time: True
      robot_radius: 0.15
      resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 10.0
          raytrace_min_range: 0.0
          obstacle_max_range: 9.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.35
      always_send_full_costmap: True

planner_server:
  ros__parameters:
    expected_planner_frequency: 10.0
    use_sim_time: True
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_smac_planner/SmacPlannerHybrid"
      
      # F1Tenth 중요 설정
      minimum_turning_radius: 0.50 # 유지
      
      # 탐색 모델: DUBINS(전진만) - 사용자 요청에 따라 유지
      motion_model_for_search: "DUBIN" 

      # 경로 평활화 (거친 격자 경로를 부드럽게)
      smooth_path: True 
      
      # 알고리즘 세부 튜닝
      downsample_costmap: false
      # 각도 양자화 빈을 줄여 계산 속도를 높이고, Smooth_path에 의존 (72 -> 48)
      angle_quantization_bins: 48 
      analytic_expansion_ratio: 3.5 
      
      # 비용 가중치 (경로의 성향 결정)
      # Dubin이므로 reverse_penalty는 큰 의미 없지만 유지
      reverse_penalty: 2.0 
      # 잦은 방향 전환 방지 페널티 유지
      change_penalty: 0.05 
      # 직진 선호 페널티 유지
      non_straight_penalty: 1.2 
      # 장애물 근처 페널티를 낮춰 경로가 덜 삐뚤빼뚤하고 맵 중앙을 따르도록 유도 (2.0 -> 1.5)
      cost_penalty: 1.5 

      # 기본 설정
      tolerance: 0.25
      allow_unknown: true
      max_iterations: 8000
      max_on_approach_iterations: 1000
      max_planning_time: 5.0